name: Jarvis FREE kit repair

on:
  push:
    branches:
      - jarvis/repo-cleanup-2026-09-05

permissions:
  contents: write

jobs:
  repair:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: jarvis/repo-cleanup-2026-09-05
          fetch-depth: 0
      - name: Repair FREE kit page
        run: python scripts/jarvis_free_repair.py
      - name: Commit repair and remove one-time files
        shell: bash
        run: |
          rm -f scripts/jarvis_free_repair.py
          rm -f .github/workflows/jarvis-one-time-repair.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "fix: align FREE kit page with verified deliverables [jarvis-auto]"
          git push origin HEAD:jarvis/repo-cleanup-2026-09-05
